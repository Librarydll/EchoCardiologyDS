<Window x:Class="EchoCardiologyDS.Views.UpdateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EchoCardiologyDS.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Редактирование" Height="250" Width="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MinHeight="132"/>
            <RowDefinition Height="auto" MinHeight="39"/>
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center" Grid.ColumnSpan="2" Margin="0 10 0 0">Редактировать</TextBlock>
        <TextBox x:Name="UpdateTB" TextWrapping="Wrap" AcceptsReturn="True" Grid.Row="0" Padding="2" FontSize="14"  Grid.Column="0" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="22,59,22,56" Background="AliceBlue" Height="26">
            
        </TextBox>
        <Button Padding="5" x:Name="UpdateButton" Margin="10" Grid.Row="1" Grid.Column="1">
        Изменить
        </Button>
    </Grid>
</Window>
